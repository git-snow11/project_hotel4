{% extends 'base.html.twig' %}

{% block title %}Facture{% endblock %}

{% block body %}
<div class="container mt-5">
    <div class="row">
        <div class="col-md-10 mx-auto">
            <div class="card">
                <div class="card-header bg-success text-white">
                    <h2 class="mb-0">🎉 Facture - Invoice</h2>
                </div>
                <div class="card-body">
                    <div class="row mb-4">
                        <div class="col-md-6">
                            <h5>Hotel Transylvania</h5>
                            <p class="mb-0">
                                123 Hotel Street<br>
                                Casablanca, Morocco<br>
                                Phone: +212 123 456 789<br>
                                Email: contact@hoteltransylvania.com
                            </p>
                        </div>
                        <div class="col-md-6 text-end">
                            <h5>Détails de la Facture</h5>
                            <p class="mb-0">
                                <strong>Facture #:</strong> {{ facturation.id }}<br>
                                <strong>Date:</strong> {{ facturation.dateFacture|date('d/m/Y') }}<br>
                                <strong>Code Réservation:</strong> <code>{{ reservation.idReservation }}</code>
                            </p>
                        </div>
                    </div>
                    
                    <hr>
                    
                    <div class="mb-4">
                        <h5>Informations Client</h5>
                        <p class="mb-0">
                            <strong>Nom:</strong> {{ app.user.prenom }} {{ app.user.nom }}<br>
                            <strong>Email:</strong> {{ app.user.email }}<br>
                            <strong>Téléphone:</strong> {{ app.user.telephone }}
                        </p>
                    </div>
                    
                    <hr>
                    
                    <h5>Détails de la Réservation</h5>
                    <table class="table table-bordered">
                        <thead class="table-light">
                            <tr>
                                <th>Description</th>
                                <th class="text-end">Détails</th>
                                <th class="text-end">Montant</th>
                            </tr>
                        </thead>
                        <tbody>
                            <tr>
                                <td>
                                    <strong>Chambre {{ reservation.chambre.chambreNumero }}</strong><br>
                                    <small class="text-muted">{{ reservation.chambre.chambreType|capitalize }} - Capacité: {{ reservation.chambre.capacite }} personnes</small>
                                </td>
                                <td class="text-end">
                                    Check-in: {{ reservation.dateDebut|date('d/m/Y') }}<br>
                                    Check-out: {{ reservation.dateFin|date('d/m/Y') }}<br>
                                    <strong>{{ reservation.dateDebut.diff(reservation.dateFin).days }} nuits</strong>
                                </td>
                                <td class="text-end">
                                    {{ reservation.chambre.prix|number_format(2, '.', ',') }} MAD/nuit<br>
                                    <strong>{{ facturation.montant|number_format(2, '.', ',') }} MAD</strong>
                                </td>
                            </tr>
                        </tbody>
                        <tfoot>
                            <tr class="table-success">
                                <td colspan="2" class="text-end"><h5 class="mb-0">Montant Total:</h5></td>
                                <td class="text-end"><h4 class="mb-0">{{ facturation.montant|number_format(2, '.', ',') }} MAD</h4></td>
                            </tr>
                        </tfoot>
                    </table>
                    
                    <div class="alert alert-info mt-4">
                        <strong>Statut du Paiement:</strong> En attente<br>
                        <small>Veuillez effectuer le paiement à votre arrivée à l'hôtel.</small>
                    </div>
                    
                    <div class="alert alert-success mt-3">
                        <strong>Statut de la Réservation:</strong> <span class="badge bg-success">{{ reservation.status|capitalize }}</span>
                    </div>
                    
                    <hr>
                    
                    <div class="row mt-4">
                        <div class="col-md-6">
                            <button onclick="window.print()" class="btn btn-secondary">
                                <i class="fa fa-print"></i> Imprimer
                            </button>
                        </div>
                        <div class="col-md-6 text-end">
                            <a href="{{ path('app_home') }}" class="btn btn-primary">
                                Retour à l'Accueil
                            </a>
                        </div>
                    </div>
                    
                    <div class="mt-4 pt-3 border-top text-center text-muted">
                        <small>
                            Merci pour votre confiance. Pour toute question, contactez-nous à contact@hoteltransylvania.com<br>
                            © 2026 Hotel Transylvania - Tous droits réservés
                        </small>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

<style>
    @media print {
        .btn, nav, footer, .alert {
            display: none !important;
        }
        .card {
            border: none !important;
        }
    }
</style>
{% endblock %}